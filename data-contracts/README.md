# Data Contracts

This directory will contain versioned JSON Schema definitions and small valid/invalid examples. Contracts must distinguish event occurrence time, ingestion time, effective time, and schema version where relevant.

The first implementation issue is `policy-event.schema.json`; it is intentionally not invented in the scaffold commit.
