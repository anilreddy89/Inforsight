# Simulator

The simulator will generate small, seeded, fictional policy-event histories and support deterministic replay. It currently contains only package metadata and a smoke-testable identity function so that the scaffold itself can be validated.

No generated dataset should be committed until its contract, assumptions, provenance fields, and data card are reviewed.
